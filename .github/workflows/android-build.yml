name: Android Debug Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Extraire la version de Gradle
        id: gradle
        run: |
          GRADLE_VERSION=$(grep distributionUrl android/gradle/wrapper/gradle-wrapper.properties | sed -E 's/.*gradle-([^-]+)-.*/\1/')
          echo "gradle-version=$GRADLE_VERSION" >> $GITHUB_OUTPUT

      - name: ☕ Choisir la bonne version de Java
        id: java
        run: |
          VERSION="${{ steps.gradle.outputs.gradle-version }}"
          if [[ "$VERSION" == 6.* ]]; then JAVA_VERSION=8;
          elif [[ "$VERSION" == 7.* ]]; then JAVA_VERSION=11;
          else JAVA_VERSION=17; fi
          echo "java-version=$JAVA_VERSION" >> $GITHUB_OUTPUT

      - name: ☕ Installer Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: ${{ steps.java.outputs.java-version }}

      - name: 📱 Installer Android SDK
        uses: android-actions/setup-android@v2

      - name: 📄 Accepter les licences Android
        run: |
          yes | sdkmanager --licenses
          sdkmanager --update

      - name: 🔧 Installer Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Installer les dépendances npm
        run: npm ci

      - name: 🗝 Générer le keystore debug (si besoin)
        run: |
          if [ ! -f android/app/debug.keystore ]; then
            mkdir -p android/app
            keytool -genkeypair \
              -v \
              -keystore android/app/debug.keystore \
              -alias androiddebugkey \
              -storepass android \
              -keypass android \
              -keyalg RSA \
              -keysize 2048 \
              -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US" \
              -deststoretype pkcs12
          fi

      - name: 🛠 Compiler l’APK Debug
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: 📤 Uploader l’APK compilé
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 7
